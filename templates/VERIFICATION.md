---
phase: "{NN-slug}"
verdict: "{pass | gaps | escalated}"
verified_at: "{ISO datetime — ship compares this against the newest code commit}"
cycle: 1                      # increments each verify pass; 3 is the budget
gaps:                         # empty list when verdict: pass
  - truth: "{the must-have truth}"
    status: "{STUB | ORPHANED | MISSING | FAILED | COULD_NOT_CHECK(<reason>) | HUMAN_NEEDED}"
    reason: "{what the ladder found}"
    missing: "{what would close it}"
accepted: []                  # gaps the user explicitly accepted or converted (COULD_NOT_CHECK needs reason + why + date, this cycle)
---

# Verification — {phase}

## Preflight

<!-- Run before spawning the blind verifier; a runtime truth whose env check
     fails becomes COULD_NOT_CHECK(reason) — do not burn a live attempt to
     rediscover it. -->

| Env check | Result |
|---|---|
| build green | {pass/fail} |
| test runner present | {pass/fail} |
| verify-env satisfiable for this phase's runtime truths | {recipe \| none-needed \| MISSING} |

## Level 0 — deterministic

| Check | Result |
|---|---|
| {test suite / build / lint} | {pass/fail + one line} |
| check:{key} (ratchet) | {pass / REGRESSION / CHECK_BROKEN} |

## Ladder (static, blind)

| Artifact | Exists | Substantive | Wired | Status |
|---|---|---|---|---|
| {path} | ✓ | ✓ | ✓ | VERIFIED |

## Truths

<!-- Final status vocabulary: VERIFIED | STATIC_ONLY | FAILED | COULD_NOT_CHECK(reason) | HUMAN_NEEDED -->
<!-- Runtime evidence entries are PATHS into phases/NN-slug/evidence/
     ({plan-or-cycle}-{slug}.{ext}) plus a ≤1-line note — not prose claims. -->
<!-- The Runtime evidence column cites paths listed in evidence/INDEX.md (generated by /potion:verify). -->
<!-- Truth rows MUST be shaped `| N. {truth text} | … |` — a numbered first cell; the INDEX citation regex depends on it. Never a separate `#` column. -->

| Truth | Static | Runtime evidence | Final |
|---|---|---|---|
| 1. {truth} | ✓ | {evidence path — note} | VERIFIED |

## Spec scenarios

<!-- delta-scoped: only requirements this phase's spec_deltas touched; same status vocabulary and promotion rules as Truths -->

| Requirement | Scenario | Static | Runtime evidence | Final |
|---|---|---|---|---|

## Summary cross-check

{Each SUMMARY's task→commit table checked against git log — OK / mismatches listed}
